<% epic = params["epic"] 
one_card = Card.where ("short_name LIKE'"+epic+"'")
id = one_card.first.id
url ="../catalog/" + id.to_s
%>

<h5>Progress Saved</h5>
<%= link_to  "Click here to continue", url %>
